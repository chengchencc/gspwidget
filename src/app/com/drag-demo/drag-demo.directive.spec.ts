import { DragDemoDirective } from './drag-demo.directive';

describe('DragDemoDirective', () => {
  it('should create an instance', () => {
    const directive = new DragDemoDirective();
    expect(directive).toBeTruthy();
  });
});
